version: 0.0
os: linux
files:
  - source: /index.html
    destination: /var/www/html/
permissions:
  - object: /var/www/html/index.html
    pattern: '**'
    owner: root
    group: root
hooks:
  AfterInstall:
    - location: scripts/start_server.sh
      timeout: 300
      runas: root
